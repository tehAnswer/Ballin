<table class="table table-ballin table-ballin-count">
  <thead>
    <tr>
      <th colspan="3" class="centered">
        <h1>{{division.name}}</h1>
      </th>
    </tr>
  </thead>
  <tbody>
    {{#each team in division.teams}}
      <tr>
        <td></td>
        <td>
          {{#link-to 'fantastic_teams.show' team.id}}
            {{team.name}}
          {{/link-to}}
        </td>
        <td>{{team.score}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>