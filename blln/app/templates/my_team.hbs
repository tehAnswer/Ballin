<div class="container">
  {{navbar-user user=model}}
   <h1> {{model.name}} </h1>


  <div class="row">
    {{#each contract in model.contracts}}
      {{partial "contracts/panel"}}
    {{/each}}
  </div>