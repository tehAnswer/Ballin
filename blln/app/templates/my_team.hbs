<div class="container">
  {{navbar-user user=model}}
   
   <div class="col-lg-12" id="nameTeamContainer">  
     <div class="thumbnail" id="nameTeam">
       <h1>{{model.name}}</h1>
     </div>
  </div>

  <div class="row">
    {{#each contract in model.contracts}}
      {{partial "contracts/panel"}}
    {{/each}}
  </div>