{{#if contract.player.isLoaded}}
  {{#if contract.team.rotation.isLoaded}}
    {{#link-to "players.show" contract.player.name}}<h3>{{contract.player.name}} <br>#{{contract.player.number}}</h3>{{/link-to}}
    <span {{bind-attr class=spanClass}} >{{currentPosition}}</span>
  {{else}}
    It's almost...
  {{/if}}
{{else}}
  Loading...
{{/if}}