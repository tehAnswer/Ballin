<div class="container">
  {{navbar-user user=nil}}



  <div class="row">
    <div class="col-md-6" id="playerInfoContainer">  
      <div class="thumbnail" id="playerInfo">
        <hr>
        <h1>{{model.name}} <small>#{{model.number}}</small></h1>
        <hr>
        <ul>
          <li><b>Height:</b> {{model.heightFormatted}} ({{model.heightCm}} cm)</li>
          <li><b>Weight:</b> {{model.weightLb}} lb ({{model.weightKg}} kg)</li>
          <li><b>Birthdate:</b> {{model.birthdate}} ({{model.birthplace}})</li>
          <li><b>Position(s):</b> {{model.canPlay}}</li>
          <li><b>Score:</b> {{model.stats.final_score}}</li>
        </ul>
      </div>
    </div>
  </div>

  {{outlet}}
</div>
